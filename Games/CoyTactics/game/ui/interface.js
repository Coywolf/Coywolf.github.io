import { engine } from "../../engine/engine";

export class Interface{

  // width is passed in, calculated from what's remaining of the canvas width after drawing the map
  // Interface will handle sizing the height of the panels/messagebox in it
  constructor(width){
    
  }
}